   /*
   @Title: Program to find the size of data type.
   @Written by: Noor Fatima.
   */
   
   #include<iostream>
   using namespace std;
   int main()
   
   {
   	cout<<"the size of int is="<< sizeof(int)<<"bytes"<<endl;
   	cout<<"the size of char is="<< sizeof(char)<<"bytes"<<endl;
   	cout<<"the size of string is="<< sizeof(string)<<"bytes"<<endl;
   	cout<<"the size of float is="<< sizeof(float)<<"bytes"<<endl;
   	cout<<"the size of bool is="<< sizeof(bool)<<"bytes"<<endl;
   	cout<<"the size of double is=0"<< sizeof(double)<<"bytes"<<endl;
   	
   	return 0;
	   
	      }
   
